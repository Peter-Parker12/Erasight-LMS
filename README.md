# Erasight LMS

Self-hosted **Moodle 5.2** (upgraded from 4.5 LTS — see phase 17), deployed via Docker Compose. This repo does not contain Moodle's source — the `Dockerfile` clones it (`MOODLE_502_STABLE` branch) at build time. What lives here is just the deployment: the Docker image definition, Compose stack, and Moodle's `config.php`.

**Webroot note**: Moodle 5.1 moved most of the codebase (every plugin directory included) under a new `public/` subdirectory. Apache's DocumentRoot points at `/var/www/html/public`, not the repo root — `config.php` and `admin/cli/*` deliberately stay at the repo root (Moodle's own restructuring left them there on purpose, via a real backward-compat shim), but any new plugin this repo adds must be `COPY`'d to `public/theme/`, `public/local/`, etc. in the `Dockerfile`, not the old top-level paths.

This replaced an earlier custom-built Next.js LMS (see git history before the Moodle migration if you need to reference that).

## Stack

- **`db`** — Postgres 16. Moodle's tables live here under the `mdl_` prefix.
- **`moodle-install`** — one-shot container that runs Moodle's DB install + admin account creation on first `docker compose up`, and configures outgoing mail from `GMAIL_USER`/`GMAIL_APP_PASSWORD`. Safe to re-run (see `docker/install-database.sh`).
- **`moodle`** — the Apache/PHP app itself, bound to `127.0.0.1:5895` only.
- **`moodle-cron`** — loops `admin/cli/cron.php` every 60s, which Moodle requires for scheduled tasks/notifications.

Public access is via **Cloudflare Tunnel** (`cloudflared`), run separately on the VPS — not part of this Compose stack. Its ingress rule points at `http://localhost:5895`.

## Theme

`docker/theme-erasight/` is a custom theme (a child of Boost — never edits Moodle core), baked into the image at `theme/erasight`. Set as the *starting* theme once, at first install, via `admin/cli/cfg.php --name=theme --set=erasight` in `install-database.sh` — deliberately **not** `$CFG->theme` in `config.php`, since that would force it and disable Site administration → Appearance → Themes entirely. The install script only sets it if `theme` has never been explicitly set at all (read-mode exit code from `cfg.php`, confirmed against its own source), so an admin's later choice via that UI sticks across future `docker compose up` runs instead of being silently overwritten back to erasight. It's being built in phases:

1. **Brand tokens** (done) — palette, Inter/Bricolage Grotesque/JetBrains Mono, radius — applied globally via `scss/pre.scss` and `scss/post.scss`, which reskins every admin screen for free since Moodle's admin UI inherits the active theme.
2. **Dashboard + catalog course cards** (done) — overrides `core_course/coursecard.mustache` and `block_myoverview/progress-bar.mustache` (Moodle's own progress display is plain text with no visual bar) for the "My courses" (`/my/`) dashboard. Moodle's separate "browse all courses" page is built from a chain of `protected` `html_writer` methods in `course/renderer.php`, not Mustache at all — rather than subclassing that (unverifiable without a live install), `docker/local-erasight/` is a small new plugin adding its own `/local/erasight/catalog.php` page, fetching courses via the stable `core_course_category` API and rendering them through the same reskinned card template. Linked into the main nav automatically via `local_erasight_extend_navigation()`.
3. **In-course "cinema mode"** (partial) — restyles Moodle's own course-index drawer (`course/format/templates/local/courseindex/*.mustache` — a real, modern Mustache-based component since Moodle 4.0, not the legacy renderer phase 2 had to work around) into a dark curriculum sidebar with a highlighted current activity, scoped to `body.pagelayout-course`. Pure SCSS, no new templates — the lowest-risk phase so far. Deliberately does **not** attempt a custom video player: Moodle has no single "video" concept to hook (a page, a URL, an H5P activity, and an embedded iframe all render completely differently), so a universal player chrome isn't something a theme can honestly claim to provide.
4. **Admin polish** (done) — `.generaltable`/`.generalbox` styling (Moodle's own long-standing, near-universal conventions for admin listings, not guessed-at page-specific markup) plus semantic status-badge colors, on top of phase 1's tokens.
5. **Easier admin navigation** (done) — two additive mechanisms, neither restructures Moodle's own admin tree:
   - `local_erasight_extend_settings_navigation()` (in `docker/local-erasight/lib.php`) injects an "Erasight quick links" branch at the bottom of the real Site Administration tree, linking straight to Course & category management, Manage users, Cohorts, and Manage enrolment plugins — each link only appears if the equivalent real entry is already visible in that admin's own (capability-filtered) tree, so a shortcut can never point somewhere they couldn't already reach the long way.
   - `docker/local-erasight/adminhome.php` is a new tile-grid landing page (`/local/erasight/adminhome.php`, gated on `moodle/site:config`) grouping those same destinations as Manage/Access/System tiles, plus a simple course/user count under Overview. Uses `pagelayout('admin')` so the real Site Administration tree (quick-links branch included) still sits in the drawer — a shortcut layer on top of Moodle's nav, not a replacement for it.
6. **Front-page hero** (done) — a full-width Udemy-style hero banner (headline, CTA, course/teammate counts) on the site's public front page. This is the one place this theme forks a shared core template rather than only adding to it: Boost's front page shares the same `theme_boost/layout/drawers.php` + `theme_boost/drawers.mustache` used by nearly every other page type, so there was no isolated template to override without affecting other pages. Instead, `theme_erasight` overrides only the `'frontpage'` entry in `$THEME->layouts` (verified via `lib/classes/output/theme_config.php` that a child theme overriding one layout key leaves every other layout — course, admin, standard, dashboard — inherited from Boost untouched) to point at its own `layout/frontpage.php` + `templates/frontpage.mustache`, near-copies of Boost's originals with one line added for the hero. **Accepted cost**: this fork won't automatically pick up any future Moodle patch to `drawers.php`/`drawers.mustache` — would need a manual diff+reapply if one ever ships.
7. **Admin explicitly follows the Udemy brand too** (done) — the earlier phase-1 decision to keep `$primary` a neutral near-black specifically so Site administration stayed quiet was reversed on request: `$primary` is now the ember accent, so every default button/focus-ring/active-nav-indicator site-wide (Home, Dashboard, My courses, Site administration included) reads as the same brand. Admin Home (`local_erasight/adminhome.php`) also gained a hero-style welcome banner, reusing the same gradient/token language as the front-page hero but contained entirely within a page this repo already owns — no new layout fork needed for that one.
8. **Full-parity welcome banners on Dashboard and My courses** (done) — `layout/mydashboard.php` and `layout/mycourses.php` are two more forked Boost layouts (`$THEME->layouts['mydashboard']`/`['mycourses']`, same partial-override mechanism as phase 6's front page), each a near-copy of `drawers.php` rendering a "Welcome back"/"Your courses" banner via the new shared `theme_erasight/banner` template (a simpler, CTA-less sibling of the front page's `hero` template) through the same `theme_erasight/frontpage` wrapper template phase 6 built — reused as-is rather than duplicated a third time, since its `{{{ herohtml }}}` insertion point was never actually frontpage-specific. **Same accepted cost as phase 6, now ×3**: none of these three forked layouts will automatically pick up a future Moodle patch to `drawers.php`/`drawers.mustache`.
9. **Dark color scheme** (done) — a `theme_erasight/colorscheme` admin setting (`settings.php`, Site administration → Appearance → Themes → Erasight), defaulting to **dark**, so it takes effect immediately on deploy without any manual step, while still being switchable back to light at any time. Reuses the exact dark palette phase 3's cinema mode already shipped (`#14181D`/`#1B2028`/`#D9DEE5`/`#2A313B`) applied site-wide instead of just course pages. `lib.php` prepends a plain `$erasight-dark` Sass boolean before both `pre.scss` and `post.scss` compile, which branch `$body-bg`/`$body-color`/`$link-color` (pre.scss) and the `:root` custom-property block (post.scss) accordingly. `--erasight-umber` deliberately stays a **fixed** value in both schemes — it's the anchor color of the hero/banner gradients and the cinema-mode background, all already-deliberately-dark bands that shouldn't change when the site-wide scheme flips; a new `--erasight-ink` token now carries the general text-color role it used to double as, and `--erasight-mist`/`--erasight-sky-deep` flip their *values* (not their names) so every existing rule referencing them adapts automatically. **Known boundary**: this covers the base page and every custom `.erasight-*` component, but native Bootstrap/Moodle widgets this theme doesn't specifically style (modals, dropdowns, raw admin forms) may still show light surfaces, since their own Bootstrap-level background variables (`$card-bg`, `$dropdown-bg`, etc.) aren't touched — a reasonable next pass if it turns out to matter in practice.
10. **Paid course storefront** (done) — real pricing and PayPal checkout, not marketing copy, on Home, the course listing, and a new Course Details page. Real course data throughout, not mock: `local_erasight_get_course_price($courseid)` (`local_erasight/lib.php`) reads a course's real `enrol_fee` instance (`enrol_get_instances()` filtered to `enrol == 'fee'`, formatted via `\core_payment\helper::get_cost_as_string()`) and returns `null` gracefully for a course with no fee instance yet, since paid enrolment is rolled out per-course, not atomically. `local_erasight_get_course_image($courseid, $renderer)` reads a real uploaded "Course image" via the file API when one exists, falling back to Moodle's own generated placeholder pattern otherwise — `catalog.php` was quietly always using the placeholder even when a real image existed; fixed as part of this phase. New: `local_erasight/course.php` — a Course Details page built the same way `catalog.php` was (stable public APIs, not `course/info.php`, which is a bare legacy non-template page) — real summary, real curriculum (`get_fast_modinfo()`), best-effort instructor (editingteacher role), and an "Enrol now" link to Moodle's own `enrol/index.php` (which hands off to `enrol_fee`'s real payment UI — no custom checkout built here). The front page gained a priced course grid plus static testimonials and a team-pricing band (`theme_erasight/landing.mustache`) below the hero — testimonials have no Moodle-native backing data, same honesty as this project's other "no native field for this" callouts elsewhere.
    - `install-database.sh` enables `enrol_fee` and `paygw_paypal` sitewide (`enrol_plugins_enabled`/`paygw_plugins_sortorder`, both confirmed real config names — `payment/gateway/` on this branch has exactly one bundled gateway) via a **read-then-append**, never a blind overwrite, since Moodle's installer already enables several enrolment methods by default. **Manual, deliberate, not automated**: the actual PayPal Business account + API credentials (Site administration → Payments → Payment accounts — a real secret), and each course's `enrol_fee` instance + price (Site administration → course → Enrolment methods — inherently per-course, not a sitewide toggle).
    - **Fixed post-deploy**: three of this phase's own Mustache comment blocks (`landing.mustache`, `frontpage.mustache`, and — pre-existing, not new this phase — `theme_erasight/templates/core_course/coursecard.mustache`) contained a literal `}}` inside their own documentation text (a JSON example, or a tag/slot name quoted for reference). Mustache comments close on the *first* `}}` they scan past, not a matched one, so each of these self-terminated early and leaked the remainder of the comment as literal page output — this is what produced the raw-JSON-looking text and tripled sections seen live. The `coursecard.mustache` instance (naming the `{{$menu}}` slot in prose) is almost certainly the real cause of an earlier, separately-reported "Unclosed section 'menu'" error on My courses, since that override renders on every course card sitewide. All three rewritten to prose that never places two `}` characters adjacent, and every custom template re-verified (a small `awk` scan confirming each comment closes exactly where intended) before pushing the fix.
11. **Course Details layout update** (done) — redesigned to match a reference Moodle storefront theme's layout, after auditing section-by-section what Moodle 4.5 actually has real data for (star ratings, "What You'll Learn"/Requirements/FAQ bullets, a preview video, and named student reviews all have no native Moodle backing — the reference page's copy for these is generic filler). Added, all backed by real or real-admin-provided data: a "NEW" badge (`course->timecreated` within the last 60 days — a display heuristic, not a stored Moodle flag); a "course includes" list showing the real lesson count, a real access-duration string (from the `enrol_fee` instance's `enrolperiod` — `0` genuinely means lifetime access, not a marketing claim), and a certificate badge shown only when the course actually contains a `certificate`/`customcert` activity (both contrib, not core-bundled, so this is detected via `get_fast_modinfo()->get_cms()`, never assumed); and three optional sections — What you'll learn / Requirements / FAQ — sourced from a real, admin-editable mechanism: three course custom fields (`core_customfield` API) auto-provisioned by `local_erasight/db/upgrade.php` (`Erasight storefront` category, textarea fields `erasight_learn`/`erasight_requirements`/`erasight_faq`), each section omitted entirely when its field is left blank rather than showing empty. Deliberately excluded: a star rating or named reviews as custom fields — an admin typing in a number/quote would misrepresent it as real aggregated feedback, the same reasoning already applied to the front page's testimonials.
    - `db/upgrade.php` runs automatically on every deploy via the existing `admin/cli/upgrade.php --non-interactive` step in `install-database.sh` (phase 10 already relies on this for registering new plugins) — no separate manual step needed, and it's idempotent (checks by name/shortname before creating anything).
    - **Fixed post-deploy, ArgumentCountError**: `db/upgrade.php`'s custom-field provisioning called `\core_customfield\api::save_field_configuration($field)` with one argument; the real signature (verified against `customfield/classes/api.php`) needs a second `$formdata` stdClass — it loops over that object's own properties (`shortname`, `name`, `configdata`, ...) and `set()`s each onto the field. Fixed by passing that second argument, matching the real implementation line-for-line.
    - **Fixed post-deploy, section duplication**: `landing.mustache` (and, latently, the new `course.mustache`) wrapped an entire section — heading included — directly in `{{#array}}...{{/array}}`, intending it as an "if non-empty" guard. Mustache has no such guard behavior for a real array: the block repeats once per element. With 3 testimonials, the whole "Student feedback" section (including its `<h3>`) rendered 3 times live; the courses section had the identical bug but looked fine only because there was exactly 1 course to iterate at the time. Fixed with separate PHP-computed booleans (`hascourses`/`hastestimonials` in `layout/frontpage.php`, `hascurriculum` in `local_erasight/course.php`) used only for the outer guard, leaving the arrays themselves for the inner per-item loop only.
    - **Fixed post-deploy, theme CSS silently not compiling at all**: the site went fully unstyled in production — no `.erasight-*` class anywhere in the compiled CSS — with zero visible error, because Moodle's `theme_config::get_css_content_from_scss()` wraps the whole SCSS compile in a bare `try { ... } catch (\Exception $e) { debugging(...); }`, and `debugging()` itself is a silent no-op unless the site's debug level is raised to Developer. Root cause, found only by calling `theme_config::load('erasight')->get_css_content()` directly with debug raised: `post.scss`'s `@import url("https://fonts.googleapis.com/...")` compiles fine under modern Sass (dart-sass), which treats a `url(...)` `@import` as a plain CSS passthrough — but Moodle bundles `scssphp`, an older/different implementation that instead tries to resolve it as a local partial file and throws "file not found for @import", silently discarding this theme's *entire* extra-SCSS block (every custom style) and falling back to serving Boost's own CSS unmodified. **Lesson**: a remote-URL `@import` inside this theme's SCSS pipeline is not safe, even though it looks and compiles fine everywhere else — never reintroduce one without checking the real compiled output on a live deploy. Fixed by loading Google Fonts via a real `<link>` tag instead, registered sitewide (not just the 3 forked layouts) through a proper Moodle 4.4+ hook listener (`classes/hook_callbacks.php` + `db/hooks.php`, listening on `core\hook\output\before_standard_head_html_generation`) rather than the older `<component>_before_standard_html_head()` lib.php callback, which still works (Moodle bridges it automatically) but logs a deprecation notice once debug is enabled.

12. **Full-bleed, chrome-free front page** (done) — two long-standing issues on the front page, only visible once the theme was actually rendering its own CSS (see phase 11's SCSS fix): the top site nav bar (`ErasightLMS` logo + menu + `Log in`) was still showing despite config.php's `$THEME->layouts['frontpage']['options']['nonavbar']` already being set, and the whole page sat boxed in a narrow centered column against a dark surround.
    - **`nonavbar` never did what its name implies** — confirmed against `lib/classes/output/core_renderer.php` on MOODLE_405_STABLE: it only sets `$header->hasnavbar`, which controls a secondary in-page header component, not the persistent top `<nav class="navbar">` bar (that comes from the `theme_boost/navbar` partial, included unconditionally by `drawers.mustache` — and by this theme's own fork of it — regardless of any layout option). Fixed with a real, theme-owned `hidenavbar` flag, set only in `layout/frontpage.php` (not `mydashboard.php`/`mycourses.php`, which build their own separate template context and keep normal navigation for logged-in users), guarding the partial include in `templates/frontpage.mustache`.
    - **The boxed appearance is `limitedwidth`**, a body class Moodle's own core `/index.php` adds unconditionally for the site front page (confirmed in real `index.php` on MOODLE_405_STABLE — not a theme or layout concern, so nothing in `config.php` could fix it), which Boost's own `layout.scss` uses to cap `#page.drawers .main-inner` to `$course-content-maxwidth`. Overridden in `post.scss`, scoped to `body.pagelayout-frontpage.limitedwidth` only, so every other `limitedwidth` page (course index, user preferences, etc.) keeps its normal readable width cap. The hero/landing sections already had their own internal max-widths (`.erasight-hero-inner` at 720px, `.erasight-landing-section` at 1100px) for readable centered text, so removing the outer constraint lets only the background bands go edge-to-edge, not the actual copy.

13. **Public catalog + Course Details, redundant page-heading removed** (done) — `local_erasight/catalog.php` and `local_erasight/course.php` no longer call `require_login()`, so anonymous visitors can browse the full course catalog and a course's pre-purchase preview page before authenticating — the standard storefront pattern (browse freely, log in only to enrol). Verified against real source (`lib/enrollib.php`) that `is_enrolled()` already returns `false` cleanly for an anonymous `$USER` (`id == 0`) rather than erroring, and `has_capability()` works the same way, so the existing hidden-course check needed no changes. **Known consequence of Moodle's own permission model, not a gap here**: a course without guest access enabled may show a sparser curriculum listing to an anonymous visitor than to an enrolled student, since `get_fast_modinfo()`'s `uservisible` flags are computed per-viewer.
    - Also fixed on the front page: `output.full_header` (Moodle's own page-heading bar) was still rendering above this theme's own hero, showing a redundant, low-contrast "sitename" heading on a plain white background — suppressed via the same `hidenavbar` flag already used to hide the persistent top nav bar, both scoped to `layout/frontpage.php` only.
    - Raised several homepage text-opacity values that read poorly against their backgrounds (hero tagline .85→.95, hero stat labels .75→.88, testimonial role text .65→.8, team-pricing body copy .8→.92) — all on the hero/landing sections specifically; course-details and admin-home opacity values were left untouched as out of scope for this pass.

14. **Richer, real front-page course cards** (done) — the landing grid's cards now show each course's real category (via `core_course_category::get()`, same pattern `catalog.php` already used) and a real short description, plain-text-stripped and hard-capped at 140 characters from the course's actual summary — not fabricated marketing copy. `core_course_category::get_courses()` needed an explicit `'summary' => true` option to return the real field at all (confirmed against `course/classes/category.php`: without it, only a boolean `hassummary` flag comes back). No `shorten_text()`-style helper exists in current core (checked — it doesn't), so the truncation is done directly with `strip_tags()` + `mb_substr()`. Also fixed the grid layout for the "only 1 course exists yet" case: `auto-fill` was leaving a single card stranded narrow-left with a wide dead space beside it; switched to `auto-fit` with a `minmax(240px, 320px)` track size, which collapses unused tracks for a lone card while still wrapping into further rows/columns exactly the same way once more real courses are added. Each card's description is `-webkit-line-clamp`'d to 3 lines so varying real summary lengths don't produce uneven card heights across a row.

15. **Landing page redesigned in a towardsai.com-inspired editorial style** (done) — requested as "match towardsai.com's layout and design." towardsai.com is a large enterprise AI-training company's marketing site; its actual content (partner logos, "500K+ practitioners" style stats, named leadership bios with photos, sector-specific case studies, a book launch) has no Erasight equivalent. When asked to "take the content from the link" for the gaps, that was declined — copying a real company's real stats, real partner logos, and real named individuals' bios onto Erasight's site would misrepresent Erasight's identity and misappropriate real people, not just fill a content gap. Rebuilt instead: the visual style and section rhythm (left-aligned hero, an emphasized-word headline, generous section spacing, a value-prop row, an audience-segmented block, short "how teams use it" cards) applied to Erasight's own real data where it exists, and new Erasight-specific placeholder copy — never lifted from towardsai.com — where it doesn't, the same honesty already applied to the existing testimonials.
    - **New sections**, all static/placeholder content via new lang strings (`theme_erasight` lang file), no invented numbers: a 3-column value-proposition row; a static two-column "For individuals / For teams" block (not interactive tabs — avoids needing a new AMD JS module for a cosmetic switcher); 3 generic "how teams use Erasight" problem/outcome cards, deliberately not framed as real client case studies (no invented company names); a closing CTA band.
    - **Deliberately excluded**: a partner/trust logo row (even fictional, a logo row visually reads as a real-partnership claim), named leadership bios with photos (identity-risk-adjacent, not ordinary marketing copy), and towardsai.com-specific structures that don't map to an LMS at all (flywheel diagram, book launch, sector-specific enterprise tabs).
    - **Hero redesign**: switched from a centered layout to left-aligned, and from a plain sitename heading to a proper headline built from three lang strings (`hero_headline_pre`/`_emphasis`/`_post`) so one word renders in an accent-colored italic span (`.erasight-emphasis`) — matching towardsai's emphasized-word headline treatment. Real stats (course/teammate counts) unchanged, still real data.

16. **Sitewide dark-mode surface fix** (done) — closes the "known boundary" phase 9 flagged: overriding `$body-bg`/`$body-color` in `pre.scss` only reaches components whose color is actually derived from those Sass variables at compile time, and this bundled Bootstrap compiles to static hex values with no runtime `--bs-*` custom properties to lean on (confirmed by inspecting the live compiled stylesheet). The top nav bar (`theme_boost/navbar.mustache` literally hardcodes `class="navbar-light bg-white"`), Bootstrap's `.card` (which wraps the Site administration page header/tabs), dropdowns, tables, and form controls all stayed white regardless of the site-wide color scheme, because none of them derive from `$body-bg` at all. Fixed in `post.scss` with direct selector overrides scoped under the existing `$erasight-dark` flag, rather than hunting down every Bootstrap Sass variable this specific bundled version happens to read.

17. **Upgraded Moodle from 4.5 LTS to 5.2 (latest stable)** (done) — a real major-version jump (4.5 → 5.0 → 5.1 → 5.2), not just picking up 4.5.x patches (which already happened automatically on every rebuild). The headline change: **Moodle 5.1 restructured the webroot**, moving most of the codebase — every plugin directory included — under a new `public/` subdirectory (confirmed by direct checks against the real `MOODLE_500_STABLE`/`501_STABLE`/`502_STABLE` branches: `theme/boost/config.php` is 404 at the old root path, 200 under `public/theme/boost/config.php` on 5.1+; the old root `index.php` now deliberately throws an error telling you to point your webserver at `public/` instead). Handled in the `Dockerfile`: plugin `COPY` destinations moved to `public/theme/erasight`, `public/local/erasight`, `public/webservice/mcp`, and Apache's DocumentRoot is now set to `/var/www/html/public` via a `sed` on the default vhost/`apache2.conf`. `config.php` and `install-database.sh`/`cron-loop.sh`'s `admin/cli/*` invocations needed **no changes at all** — confirmed via Moodle's own official 5.1 restructure guide that `$CFG->dirroot`/`$CFG->wwwroot` behave as before, and a real backward-compat shim at the old root `lib/setup.php` (a genuine 29-line file, not assumed) bridges an old-style root `config.php` through to the real code now under `public/lib/setup.php`.
    - Spot-checked the real `UPGRADING.md` changelogs across 5.0/5.1/5.2 for breaking changes touching this codebase's actual API usage (the SCSS pre/extra-scss callback mechanism, the `core\hook\output\before_standard_head_html_generation` hook, the `customfield_textarea` field type, `enrol_get_instances`/`core_payment\helper::get_cost_as_string`, `get_fast_modinfo`, `is_enrolled`) — none appear as changed or removed.
    - The third-party `webservice_mcp` plugin (CRM/Claude integration) states "Moodle 4.5 or higher" in its own README with no stated upper bound — reasonable evidence of compatibility, but never independently verified against 5.2's actual API surface; **re-test the MCP integration end-to-end after this deploy**.
    - **Deliberate risk accepted, not hidden**: this was a direct-to-production upgrade (full DB + `moodledata` backup taken first, not a staged rollout in an isolated copy first) — a real choice made explicitly aware that a database schema upgrade cannot be undone by reverting the `Dockerfile`'s branch alone; the backup is the actual rollback path if something goes wrong, not a code revert.

All seventeen phases are now in place. Still open: a 502 was reported when creating a course category (`course/editcategory.php`) shortly after phase 4's deploy — root cause not yet identified, needs `docker compose logs moodle` from around that request to diagnose (could be one of these plugins, could be unrelated VPS resource pressure; not established either way).

New plugins (theme or local) aren't picked up by an *existing* install the way a fresh `install_database.php` run picks up everything present at once — `docker/install-database.sh` now also runs `admin/cli/upgrade.php --non-interactive` on every `docker compose up` to register them (confirmed idempotent: exits 0 when nothing's pending, not an error).

## CRM integration (Twenty CRM ↔ Moodle)

Two capabilities exposed to the (modified) Twenty CRM, both through Moodle's official Web Services REST API rather than custom endpoints, so no new code in this repo has to be trusted with account creation or course data:
1. List courses and their info.
2. Create a student account when a lead converts.

**What's automated** (`docker/install-database.sh` + `docker/local-erasight/db/services.php`):
- `enablewebservices` and `webserviceprotocols=rest` are turned on idempotently on every `docker compose up`, same pattern as the SMTP settings.
- A narrowly-scoped external service, **"Erasight CRM integration"** (shortname `erasight_crm_integration`), bundling exactly two core functions — `core_user_create_users` and `core_course_get_courses` — not a general-purpose API token. `restrictedusers => 1` means enabling it isn't enough by itself; a specific user still has to be explicitly authorised (next section).

**What's manual, deliberately** (a token is a real secret — this repo doesn't generate or store one):
1. Log in as admin → Site administration → Server → Web services → External services → find **Erasight CRM integration** → **Authorised users** → add a user for this integration to act as. Recommend a **dedicated account**, not an existing admin — least privilege, and it makes the audit trail (who created a given account) clean.
2. That account needs two things granted, since it isn't a real enrolled user in every course:
   - `moodle/user:create` at system level, for account creation.
   - `moodle/course:view` at system level, for `core_course_get_courses` to return every course (each course checks this capability in its own context — confirmed in `course/externallib.php` — and a capability allowed at system context cascades down to every course context beneath it). The straightforward way to grant both: Site administration → Users → Permissions → Define roles → create a role (e.g. "CRM integration") with just those two capabilities allowed, then Assign system roles → assign it to the dedicated account. (Assigning the built-in "Manager" role instead would also work and is less setup, but grants far more than these two calls need.)
3. Site administration → Server → Web services → Manage tokens → create a new token for that user, scoped to the **Erasight CRM integration** service.
4. Store that token wherever your Twenty CRM fork keeps its other API credentials.

**Listing courses** — wherever the CRM needs course data (e.g. to show what's available before a lead converts):

```
POST https://lms.erasight.net/webservice/rest/server.php
Content-Type: application/x-www-form-urlencoded

wstoken=<TOKEN>
wsfunction=core_course_get_courses
moodlewsrestformat=json
```

No parameters needed beyond that — omitting `options[ids]` returns every course except the site's own front-page pseudo-course. Each result includes `id`, `fullname`, `shortname`, `summary`, `startdate`/`enddate`, `format`, `categoryid`, and more (full field list in `course/externallib.php`'s `get_courses_returns()`).

**Creating a student account**, in the CRM's lead-conversion code path (not in this repo — that's the Twenty CRM fork's own codebase):

```
POST https://lms.erasight.net/webservice/rest/server.php
Content-Type: application/x-www-form-urlencoded

wstoken=<TOKEN>
wsfunction=core_user_create_users
moodlewsrestformat=json
users[0][username]=<email, lowercased>
users[0][firstname]=<lead first name>
users[0][lastname]=<lead last name>
users[0][email]=<lead email>
users[0][auth]=manual
users[0][createpassword]=1
```

- `createpassword=1` means Moodle generates the password itself and emails the new user a set-password link — the CRM never needs to invent or transmit a real password.
- `username` must be unique. Using the lowercased email is the simplest reliable choice unless the CRM has a better natural key already.
- **Not idempotent** — calling this twice for the same email throws `Username already exists` (confirmed in `user/externallib.php`). If the CRM's conversion trigger can fire more than once for the same lead (retries, re-conversion, etc.), that specific error response should be treated as "already handled," not a failure — worth handling on the CRM side since this repo doesn't own that code.

This creates the *account* only — it does not enrol the student into any course. Enrolling into a specific course (e.g. whatever the lead converted for) would need a third call, `enrol_manual_enrol_users` (not yet added to the service) — say if that's wanted next.

## Claude ↔ Moodle (MCP)

Lets Claude (Claude Code, Claude Desktop, or claude.ai) query and manage this LMS directly from a chat, via [`webservice_mcp`](https://github.com/onbirdev/moodle-webservice_mcp) — a third-party but actively-maintained Moodle plugin (confirmed the canonical repo: 15 stars, most recently pushed, vs. one stale fork) that turns Moodle's own external services into a real MCP server. Cloned fresh at build time (see `Dockerfile`), same reasoning as Moodle core itself — not vendored into this repo.

**What's automated**: the plugin itself (cloned + installed via the existing `admin/cli/upgrade.php` step), the `mcp` web service protocol (`webserviceprotocols=rest,mcp` in `install-database.sh`), and a scoped external service, **"Erasight Claude integration"** (shortname `erasight_claude_integration`, in `local_erasight/db/services.php`) — a separate service from the CRM's, its own token/audit trail, even though it starts scoped to the same two functions (`core_user_create_users`, `core_course_get_courses`). Add more function names to that array (verified against the relevant `externallib.php` first) to give Claude more capability later.

**What's manual, deliberately** (same reasoning as the CRM integration — a token is a real secret):
1. Site administration → Server → Web services → External services → **Erasight Claude integration** → Authorised users → add a dedicated account (not an existing admin).
2. That account needs `webservice/mcp:use` granted, alongside the same `moodle/user:create` + `moodle/course:view` the CRM integration's account needs (same custom-role approach as before — see the CRM section above).
3. Site administration → Server → Web services → Manage tokens → create a token for that user, scoped to **Erasight Claude integration**.

**Connecting Claude Code to it**, once you have the token:

```bash
claude mcp add --transport http erasight-lms "https://lms.erasight.net/webservice/mcp/server.php?wstoken=<TOKEN>"
```

(Add `--scope user` if you want this available across every project, not just this one; default scope is local/this-project-only.) Verify with `claude mcp list`. The plugin also accepts the token as an `Authorization: Bearer <TOKEN>` header instead of a query parameter, if you'd rather not have it embedded in a URL — `claude mcp add --transport http erasight-lms https://lms.erasight.net/webservice/mcp/server.php --header "Authorization: Bearer <TOKEN>"`.

## Deploy

```bash
cp .env.example .env   # fill in POSTGRES_PASSWORD, NEXT_PUBLIC_APP_URL, GMAIL_*, INITIAL_ADMIN_*
docker compose up -d --build
```

A single `docker compose up` brings up `db` → installs Moodle → starts `moodle` + `moodle-cron`, with no manual install-wizard step.

## Known gaps to close inside Moodle once it's up

- Install the [`enrol_invitation`](https://github.com/michael-milette/moodle-enrol_invitation) plugin for email-based course invitations (closest native equivalent to the old app's invite flow — one course per invitation, not multi-class in one email).
- S3-compatible file storage (reusing the VPS's existing MinIO) isn't wired up yet — Moodle uses local-disk `moodledata` for now. Add the `tool_objectfs` plugin later if that becomes necessary.
